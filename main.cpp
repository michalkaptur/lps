#include <iostream>
#include <string>

unsigned longest_palindrome_substring(const std::string& input){
    return 0;
}

int main() {
    std::string input("mightyracertwo");
    std::cout << "Longest substring palindrome of " << input
              << " has lenght:" << longest_palindrome_substring(input);
    return 0;
}
